services:
  mongo:
    image: mongo:7
    container_name: splitly-mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: project_fcaj
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
